Method 1 to generate simulated data:

Use the followign command to generate simulated data (small and large)

1. # generate simulated Y (label)

python generate_simulated_Y.py > y_name


2. # generate a simulated dataset with n samles and p features

python generate_simulated_data.py 100 5500 > simulated_data_100_5500.txt


============

Method 2 (multivariate probit model) to generate simulated data:


1. # generate initial simulated Y and top level X values (numeric)

python generated_simulated_mvp_Y.py > mvp_y_fname

Two files will be generated: mvp_y_fname and top_x_vals


2. # generated a simulated dataset with n samples and p features

python mvp_generated_simulated_data.py 100 5500 > simulated_data_100_5500.txt



3. # run simulation: first generates 100 simuated datasets and then run feature selections

python run_simulation.py 100 5500 > 100_5500_tpr_fpr_time.txt
